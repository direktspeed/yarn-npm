{
  "name": "npm-yarn",
  "version": "1.0.9",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "publish": "git push origin --all && git push origin --tags",
    "release:prerelease": "npm --new-version prerelease && npm publish",
    "release:patch": "npm --new-version patch && npm publish",
    "release:minor": "npm --new-version minor && npm publish",
    "release:major": "npm --new-version major && npm publish"
  },
  "bin": {
    "npm": "bin/npm"
  },
  "devDependencies": {
    "yarn": "^0.16.1"
  }
}
